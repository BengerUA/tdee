import React from 'react'
import TdeeCalculator from '../components/Calculator'

type Props = {}

const Header = (props: Props) => {
    return (
        <div>
            <TdeeCalculator />
        </div>
    )
}

export default Header
